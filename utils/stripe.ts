import { STRIPE_PARTNER_DETAILS } from "./constants";

// const nuxtApp = useNuxtApp()
// nuxtApp.provide('stripe', () => {
//   const pk = useRuntimeConfig().public?.cyStripePk as string || ''
//   if(pk && process.client){
//     window?.Stripe(pk, {})
//   }
// })

